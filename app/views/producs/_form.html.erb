<%= form_for(@produc) do |f| %>
  <% if @produc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@produc.errors.count, "error") %> prohibited this produc from being saved:</h2>

      <ul>
      <% @produc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
